---

# Default configuration. Override at:
# /etc/osp/osp.yml

osp:
  corpus: XXX
  bin: XXX

hlom:
  corpus: XXX

tika:
  server: XXX

postgres:

  default:
    args:
      database: osp
      host: localhost
      port: 5432
      user: postgres

  test:
    args:
      database: osp-test

redis:
  host: localhost
  port: 6379
  db: 0

elasticsearch:
  host: localhost
  port: 9200
