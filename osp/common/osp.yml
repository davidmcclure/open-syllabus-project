---

# Default OSP configuration. Copy to /etc/osp/osp.yml

postgres:
  host: localhost
  port: 5432
  user: postgres
  password:

redis:
  host: localhost
  port: 6379
  db: 0

elasticsearch:
  host: localhost
  port: 9200
