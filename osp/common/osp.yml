---

# Default configuration. Override at:
#
# /etc/osp/osp.yml
# ~/osp.yml
# ./osp.yml

osp:
  corpus: XXX
  worker: XXX

hlom:
  corpus: XXX

overview:
  api_token: XXX
  api_url: https://www.overviewproject.org/api/v1
  set_id: XXX

tika:
  server: XXX

postgres:

  default:
    args:
      database: osp
      host: localhost
      port: 5432
      user: postgres

  test:
    args:
      database: osp-test

redis:
  host: localhost
  port: 6379
  db: 0

elasticsearch:
  host: localhost
  port: 9200
