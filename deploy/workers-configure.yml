---

- name: Configure an OSP worker instance
  sudo: yes
  hosts: tag_osp_worker

  vars_files:
    - vars/elasticsearch.yml
    - vars/postgres.yml

  roles:
    - common
    - postgres
    - elasticsearch
    - redis
    - supervisor
    - nginx
