---

- name: Configure OSP workers
  become: yes

  hosts:
    - tag_osp_server
    - tag_osp_worker

  vars_files:
    - vars/postgres.yml

  roles:
    - common
    - postgres
    - elasticsearch
    - redis
    - supervisor
    - nginx
