---

osp:
  corpus: /osp/repository
  bin: /home/{{ ansible_ssh_user }}/osp/env/bin

hlom:
  corpus: /osp/hlom

tika:
  server: http://localhost:9998/tika

overview:
  api_token: {{ overview_api_token }}
  set_id: {{ overview_set_id }}

postgres:

  default:
    args:
      database: osp
      host: localhost
      port: 5432
      user: osp
      password: {{ osp_password }}

  remote:
    args:
      host: {{ osp_data_host }}
    tables:
      - document_text
      - document_date_archive_url
      - document_date_semester
      - document_date_file_metadata
      - hlom_citation

redis:
  host: localhost
  port: 6379
  db: 0

elasticsearch:
  host: localhost
  port: 9200
